{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "functions": {
        "backend/server.js": {
            "memory": 512,
            "maxDuration": 30
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/backend/server.js"
        }
    ]
}